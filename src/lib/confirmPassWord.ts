export const confirm_password = (input:any) => {
    let isValid = true;
    console.log(input);
    // console.log(password, confirmPassword);
    // if (password.toLowerCase() === confirmPassword.toLowerCase()) {
    //     return true;
    // }
    // return false;
    return false;
}